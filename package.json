{
  "scripts": {
    "build": "parcel build src/*.ts",
    "watch": "parcel watch src/*.ts"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.180",
    "parcel": "^2.0.0",
    "typescript": "^4.6.3"
  }
}
